services:
  db:
    image: postgres:18
    container_name: sitespeak-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sitespeak
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
      - ./schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
      - ./1-seed_supplier.sql:/docker-entrypoint-initdb.d/02-seed_supplier.sql
      - ./2-seed_equipment.sql:/docker-entrypoint-initdb.d/03-seed_equipment.sql
      - ./2-seed_material.sql:/docker-entrypoint-initdb.d/04-seed_material.sql
      - ./3-seed_basic.sql:/docker-entrypoint-initdb.d/05-seed_basic.sql

  client:
    image: node:20-alpine
    container_name: sitespeak-client
    ports:
      - "5173:5173"
    working_dir: /app
    environment:
      - NODE_ENV=development
    volumes:
      - ./client:/app
      - /app/node_modules
    command: >-
      sh -c "npm install -g pnpm && pnpm i && pnpm run dev --host 0.0.0.0"
    depends_on:
      - db

volumes:
  postgres_data:
